# Example environment variables for BaseX OracleView
# Rename to .env and fill in real values

# YouTube Data API key
YOUTUBE_API_KEY=

# OpenAI (optional) — original API key
OPENAI_API_KEY=

# Optional override: OpenAI-shaped endpoint. If set to http://localhost:4567 the groq-proxy will be used.
OPENAI_API_URL=

# GROQ provider config (if you want to use GROQ via the local proxy)
GROQ_PROVIDER_URL=
GROQ_API_KEY=

# RPC and contract addresses used by the backend
SEPOLIA_RPC_URL=
SEPOLIA_CONTRACT_ADDRESS=
BASE_RPC_URL=
BASE_CONTRACT_ADDRESS=

# Private key (oracle-service only) — keep secret
PRIVATE_KEY=

# Server port (optional)
PORT=3000
